//var cdir = point_direction(oPlayer.x, oPlayer.y, x, y);
//var tdir = point_direction(oPlayer.x, oPlayer.y, mouse_x, mouse_y);

//image_speed = 0.5;

//Speed = sign(mouse_x);


//// Orbital motion
//Angle += clamp(angle_difference(tdir, cdir), -Speed, Speed);
//image_xscale = -1;
//// Update position
//x = lengthdir_x(0, Angle) + oPlayer.x;
//y = lengthdir_y(0, Angle) + oPlayer.y;
//image_angle = point_direction(x, y, mouse_x, mouse_y);